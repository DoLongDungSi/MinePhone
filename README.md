# How to run

# Cài đặt
Trên Arch-basedùng, bạn có thể dùng yay để cài đặt:

```bash
yay -S npm docker docker-compose python
```

Nếu trên Windows, bạn nên cài đặt Docker thủ công

# Chạy

```bash
docker compose up --build
```